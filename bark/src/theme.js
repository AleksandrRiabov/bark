import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#23577c", 
    },
    text: {
      dark: "#333",
      main: "#23577c",
    },
  },
});

export default theme;